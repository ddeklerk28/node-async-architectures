{
  "name": "@forge/core",
  "version": "1.0.0",
  "description": "Core abstractions and interfaces for async architecture patterns",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "async",
    "core",
    "types",
    "base-classes"
  ],
  "author": "dirk.deklerk",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.2",
    "vitest": "^1.0.0"
  },
  "files": [
    "dist"
  ]
}

